# 服务端口
server.port=9111

# 连接数据库的相关配置
spring.datasource.url=jdbc:mysql://localhost:3306/galaxy?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
spring.datasource.username=root
spring.datasource.password=root

# 响应结果中，不显示为null的属性
spring.jackson.default-property-inclusion=non_null

# 配置SQL的XML文件的位置
mybatis.mapper-locations=classpath:mapper/*.xml

# 开启Knife4j的增强模式
knife4j.enable=true

# 日志的显示级别
logging.level.cn.tedu.galaxy=trace

# 当前项目的自定义配置：JWT使用的secretKey
galaxy.jwt.secret-key=97iuFDVDfv97iuk534Tht3KJR89kBGFSBgfds
# 当前项目的自定义配置：JWT的有效时长，以分钟为单位
galaxy.jwt.duration-in-minute=10000
